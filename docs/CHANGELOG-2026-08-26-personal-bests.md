# Personal Bests — shipped record

Historical Personal Bests was fully implemented and shipped before this entry was created. The feature includes the `personal_bests` table, `/api/personal-bests` CRUD API, PR/baseline integration, and the `/personal-bests` page.

The implementation landed on `main` in commits `3eb468e`, `8af065e`, `82f164b`, and `c1ff0ab` and is live. The design reference is `docs/superpowers/specs/2026-08-17-personal-bests-design.md`.

This standalone history file closes the documentation gap identified during the 2026-08-26 backlog audit: the feature had shipped, but its shipped status had not been recorded in the repository's durable documentation.