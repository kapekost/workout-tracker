# Improvements Log

<!-- last-reviewed-count: 11 -->

Append one line per entry via `scripts/append_improvement.sh <local|template|unsure> "<note>"` — do
not edit this file by hand except to resolve a conflict. Reviewed automatically at the end of any
`/orchestrate` tick that added a new entry (see `PLAYBOOK.md` step 8); entries before the
`last-reviewed-count` marker above are never re-scanned.

## Log
- [unsure] 2026-08-30: code-review skill (Skill tool, forked execution) silently reviewed a completely different attached repo (kapekost-web, not workout-tracker) when invoked with no explicit target in this multi-repo session — had to re-invoke with an explicit '<path> diff <base>...<head>' arg to get a correct result. Not a workout-tracker doc/config fix; likely a Claude Code harness/skill-runtime behavior (cwd/context not scoped per-repo for forked skill execution).
- [local] 2026-08-30: Feature intake's ready-vs-split binary (PLAYBOOK.md step 3 / Feature intake section) has no path for 'owner Q&A shaped it, but it still needs a written spec before ready' — hit repeatedly this session (#27, #30, #32, #33 all landed here, unlike #29 whose answers were concrete enough to split directly into child issues). Worked around it ad hoc each time (comment + DECISIONS.md entry, stays intake, note 'needs a spec pass'). Fixed via PR #71 — PLAYBOOK.md now names this as a third Feature Intake outcome.
- [unsure] 2026-08-31: The Agent tool (subagent dispatch), when called without worktree isolation, shares the same working directory/git checkout as the parent session by default. Dispatched a background subagent to execute #66's plan without isolation:'worktree'; it ran 'git checkout -b ...' in what turned out to be the shared directory, silently switching the parent orchestrator's checked-out branch mid-session (caught via a stale-STATE.md-content system-reminder, not by anything failing loudly). Worked around it by creating separate 'git worktree add' checkouts (plain git, not the harness EnterWorktree tool, which is explicitly restricted to explicit user/CLAUDE.md requests) for the controller's own concurrent work. Not fixable via a PR in this repo or the template repo — looks like Agent-tool/harness default behavior. Real fix candidate: PLAYBOOK.md's Execute step should default to isolation:'worktree' for any subagent dispatch that does its own git branch/commit work, not just plain source edits.
- [template] 2026-09-02: Children created during an intake split can land with no state label at all, making them invisible to both tracks. PLAYBOOK's Feature-intake step 3 requires children be 'type/priority/effort labeled' and never names a state label; the ISSUE_TEMPLATE forms default to 'intake' but only for UI-created Issues, and 'gh issue create' during a split bypasses the form entirely. Real case: #68 (workout-tracker) was created 2026-08-30 alongside #66/#67/#69, got type/priority/effort but no ready/intake, and was skipped by both 'gh issue list --label ready' and intake triage for 3 days. Fix: (a) step 3 must require a state label on every child, (b) step 2's reconcile must query for open Issues carrying no state label.
- [template] 2026-09-04: Enabling GitHub's 'Automatically delete head branches' silently deletes the orchestration home branch (claude/<repo>-backlog-*) the moment its PR lands on main — taking the PLAYBOOK claim mechanism with it. Hit for real on workout-tracker 2026-09-04: PR #90 was merged deliberately WITHOUT --delete-branch, but the repo-level setting deleted it anyway, so the branch other ticks check for In-flight claims simply ceased to exist. Recovered by re-pushing the local ref, which only worked because a local copy happened to still exist. Note the irony: enabling that setting had just closed a long-standing 'Needs owner' item about stray branches. Fix candidates: (a) PLAYBOOK must require re-pushing the home branch immediately after any home-branch PR merges, and verifying it exists as part of the tick's close-out; (b) better, stop merging the home branch to main at all and instead cherry-pick its doc commits onto a short-lived branch, leaving the home branch permanently unmerged and therefore never auto-deleted.
- [local] 2026-09-04: PLAYBOOK step 4 tells dispatched subagents to run scripts/append_improvement.sh inline, but that script writes docs/orchestration/IMPROVEMENTS.md, which a feature branch must never touch — the two instructions contradict each other. Hit on the #88 tick 2026-09-04: the subagent correctly refused to run it rather than dirty a forbidden path, and reported its friction in its final message instead, where the controller had to re-log it by hand. Fix: step 4 should ask subagents to report friction in their result and make logging it the controller's job from the home branch, or give the script a mode that stages the note somewhere a feature branch may write.
- [local] 2026-09-04: The verification commands the controller hands a subagent ('cd backend && .venv/bin/python -m pytest', 'cd frontend && npm test') only work in the main checkout. A subagent dispatched with isolation:'worktree' gets a fresh worktree with no backend/.venv and no frontend/node_modules, so both fail as written. Cost real time on the #88 tick 2026-09-04. Fix: AGENTS.md should give the venv-less invocation, and PLAYBOOK's Execute step should tell the controller to pass the main checkout's absolute interpreter path (or an 'npm ci first' note) whenever it dispatches into a worktree.
- [unsure] 2026-09-04: Common tooling is missing from the PATH the Bash tool starts with in this session (/usr/bin:/bin:/usr/sbin:/sbin plus plugin bins): gh is at /opt/homebrew/bin, docker at /usr/local/bin (Docker Desktop symlink), node/npm under ~/.nvm/versions/node/*/bin. Every invocation needs an explicit PATH export or absolute path, and 'command not found' is the only clue. Hit by both the controller and the subagent on the #88 tick 2026-09-04. Not a repo fix — harness/sandbox environment behaviour.
- [local] 2026-09-05: AGENTS.md's backend setup says 'python3 -m venv .venv', but macOS's system python3 is 3.9.6 and the pinned requirements need 3.14 (pip resolves nothing: 'Could not find a version that satisfies fastapi==0.141.1'). Cost a full venv build plus a rebuild on the #93 tick 2026-09-05. The Dockerfile pins python:3.14-slim and Homebrew provides /opt/homebrew/bin/python3.14, so the setup block should name the interpreter version rather than whatever 'python3' happens to be.
- [template] 2026-09-05: A dispatched subagent that dies mid-task on an API/session rate limit (429) leaves no report, and PLAYBOOK has nothing to say about recovering from it. Real case, #93 tick 2026-09-05: the subagent had written its RED tests and was about to run them when it hit the session limit; the work survived only as uncommitted changes in its isolation worktree, and the controller's own In-flight claim was still live and correctly still its own. Fix: PLAYBOOK's Execute step should say that on any abnormal subagent termination the controller must (a) inspect the subagent's worktree for uncommitted work before re-dispatching, since re-running from scratch silently discards it, and (b) treat the claim as still held rather than reading it as another driver's.
- [template] 2026-09-05: The /orchestrate approve variant does not say which branch its record goes on, and the 2026-09-04 'home branch never merges to main' decision made that ambiguity load-bearing. Real case, workout-tracker 2026-09-05: the owner's '/orchestrate approve 84' added the label, commented the Issue, and also committed the approval to docs/orchestration/STATE.md on main — but main's STATE.md predated the #88/#93 tick entries that live only on the home branch, so the repo ended up with two divergent STATE.md files, each holding facts the other lacked. The next tick had to reconcile them by hand before it could claim anything. Fix: PLAYBOOK's approve variant (and the /orchestrate command doc) must say that any orchestration-doc write lands on the home branch, never the default branch — the same rule the merge decision already implies for every other doc commit.
- [template] 2026-09-05: PLAYBOOK step 1 tells a tick to read STATE.md/GUARDRAILS.md/DECISIONS.md but never says from which branch, so it reads the working tree — the default branch. Combined with the 2026-09-04 'home branch never merges to main' decision, main's copies are a hand-cherry-picked subset that lags silently. Real case, workout-tracker 2026-09-05: two consecutive ticks (PR #113, then the next tick) read main's DECISIONS.md, found no standing-approval entry, and both reported the accounts chain blocked on an owner approval that had been recorded on the home branch hours earlier and explicitly named #105/#86/#87. main was six DECISIONS entries and 28 STATE.md commits behind; it surfaced only because clearing an In-flight claim forced a merge that exposed the divergence. Note this is the read-side twin of the 2026-09-05 approve-variant entry above, which covers the write side. Fixed locally in PR #116 (step 1 now names the branch and gives the git show command); the template has the same gap.
